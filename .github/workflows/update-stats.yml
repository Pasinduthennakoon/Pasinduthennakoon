name: Update GitHub Stats & Activity

on:
  schedule:
    - cron: '0 0 * * *'  # daily
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Generate GitHub Stats SVG
        uses: anuraghazra/github-readme-stats@v0.2.0
        with:
          username: 'Pasinduthennakoon'
          token: ${{ secrets.GITHUB_TOKEN }}
          show_icons: true
          hide_border: true
          theme: 'dark'
          output: 'github-stats.svg'

      - name: Generate Activity Graph SVG
        uses: nicolasbeauvais/github-activity-graph@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          username: 'Pasinduthennakoon'
          theme: 'high-contrast'
          output: 'activity-graph.svg'
          area: true
          line: '58a6ff'
          point: '58a6ff'
          area_color: '58a6ff'
          hide_border: true

      - name: Commit and push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add github-stats.svg activity-graph.svg
          git commit -m "Update stats and activity graph" || echo "No changes"
          git push
